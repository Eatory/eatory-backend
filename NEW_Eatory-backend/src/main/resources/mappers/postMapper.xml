<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eatory.mvc.model.dao.PostDao">

	<!-- 게시글 삽입 -->
    <insert id="insertPost" parameterType="PostDTO" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO `Post` (user_id, post_time, image, content)
        VALUES (#{userId}, NOW(), #{image}, #{content});
    </insert>

    <!-- 게시글 조회 -->
    <select id="selectPostById" parameterType="long" resultType="PostDTO">
        SELECT * FROM `Post` WHERE post_id = #{postId};
    </select>

    <!-- 전체 게시글 조회 -->
    <select id="selectAllPosts" resultType="PostDTO">
        SELECT * FROM `Post` ORDER BY post_time DESC;
    </select>

    <!-- 게시글 수정 -->
    <update id="updatePost" parameterType="PostDTO">
        UPDATE `Post`
        SET image = #{image}, content = #{content}, post_time = NOW()
        WHERE post_id = #{postId};
    </update>

    <!-- 게시글 삭제 -->
    <delete id="deletePost" parameterType="long">
        DELETE FROM `Post` WHERE post_id = #{postId};
    </delete>


</mapper>